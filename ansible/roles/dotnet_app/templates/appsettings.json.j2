{#- Non-secret settings only: the common block overlaid by the per-app one.
    Rendered as JSON so a mistake is a template failure rather than a runtime
    surprise. -#}
{{ (app_settings_common | combine(app_settings[item.key] | default({}), recursive=True)) | to_nice_json }}

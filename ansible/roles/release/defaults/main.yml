---
# show | list | promote | rollback | none
# `none` installs the CLI and stops -- that is how deploy.yml makes sure
# `pscloud` is on the box before it calls it.
release_action: none

release_venv: /opt/pscloud/venv
release_from_env: rc
release_to_env: prod
release_tag: ""
